# Misago Forum - Fixed Production Deployment

**Автор**: MiniMax Agent  
**Версия**: 1.1 (Исправленная)  
**Дата**: 28 ноября 2025  
**Лицензия**: GPL-2.0

## 🎯 Что исправлено в этой версии

### ✅ Критические исправления ошибок

1. **Исправлена опечатка в nginx конфигурации**
   - Было: `www.benj.run_place`
   - Стало: `www.benj.run.place`

2. **Исправлена переменная в docker-compose.prod.yaml**
   - Было: `POSTGROS_PASSWORD`
   - Стало: `POSTGRES_PASSWORD`

3. **Создана структура nginx директорий**
   - Добавлена папка `nginx/sites-enabled/`
   - Создан симлинк на конфигурацию

4. **Улучшены скрипты деплоя**
   - Новый `deploy_fixed.sh` с расширенной проверкой ошибок
   - Новый `quick_deploy_fixed.sh` для автоматического развертывания

5. **Добавлена пошаговая инструкция**
   - Подробное руководство на русском языке
   - Автоматизация процесса развертывания

## 🚀 Быстрый старт

### Автоматическое развертывание
```bash
# Скачать и запустить автоматический скрипт
curl -O https://raw.githubusercontent.com/borizzz93/brp2/main/quick_deploy_fixed.sh
chmod +x quick_deploy_fixed.sh
./quick_deploy_fixed.sh
```

### Ручное развертывание
```bash
# 1. Проверка системы
./deploy_fixed.sh check

# 2. Настройка окружения
./deploy_fixed.sh setup

# 3. Развертывание
./deploy_fixed.sh deploy
```

## 📁 Структура проекта

```
misago/
├── 🐳 Docker файлы
│   ├── Dockerfile.prod          # Production Docker образ
│   ├── docker-compose.prod.yaml # Основной compose файл
│   └── docker-compose.prod.improved.yaml # Альтернативный с резервными портами
│
├── 🔧 Скрипты деплоя
│   ├── deploy_fixed.sh          # 🆕 Исправленный основной скрипт
│   ├── quick_deploy_fixed.sh    # 🆕 Автоматическое развертывание
│   ├── fix_and_deploy.sh        # Исправление и деплой
│   └── quick_deploy.sh          # Быстрый деплой
│
├── 🌐 Web сервер
│   └── nginx/
│       ├── nginx.conf           # Основная конфигурация nginx
│       ├── sites-available/misago # 🆕 Конфигурация сайта (исправлена)
│       └── sites-enabled/       # 🆕 Симлинк на конфигурацию
│
├── 📚 Документация
│   ├── DEPLOYMENT_GUIDE_RU.md   # 🆕 Пошаговая инструкция
│   ├── README.md                # Основной README
│   └── DEPLOYMENT.md            # Инструкция по деплою
│
└── ⚙️ Конфигурация
    ├── .env.example             # Пример переменных окружения
    └── requirements.prod.txt    # Production зависимости
```

## 🔧 Технологии

- **Backend**: Python 3.12, Django, Celery
- **Frontend**: React.js, Webpack
- **База данных**: PostgreSQL 15
- **Кэш**: Redis 7
- **Веб-сервер**: Nginx 1.25
- **Контейнеризация**: Docker + Docker Compose
- **Мониторинг**: Nginx Prometheus Exporter

## 📊 Системные требования

- **ОС**: Ubuntu 22.04 LTS
- **RAM**: Минимум 4GB (рекомендуется 8GB+)
- **Диск**: Минимум 20GB свободного места
- **CPU**: 2+ ядра
- **Сеть**: Статический IP адрес

## 🛠️ Основные команды

```bash
# Проверка статуса сервисов
./deploy_fixed.sh status

# Просмотр логов
./deploy_fixed.sh logs [service]

# Перезапуск сервисов
./deploy_fixed.sh restart

# Создание бэкапа
./deploy_fixed.sh backup

# Проверка здоровья системы
./deploy_fixed.sh health

# Мониторинг ресурсов
./deploy_fixed.sh monitor
```

## 🔒 Безопасность

### SSL сертификат
```bash
# Установка Let's Encrypt
sudo apt install certbot
sudo certbot certonly --standalone -d benj.run.place -d www.benj.run.place

# Копирование сертификатов
sudo cp /etc/letsencrypt/live/benj.run.place/fullchain.pem ./nginx/ssl/misago.crt
sudo cp /etc/letsencrypt/live/benj.run.place/privkey.pem ./nginx/ssl/misago.key
```

### Firewall
```bash
sudo ufw allow OpenSSH
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 8080/tcp
sudo ufw --force enable
```

## 📧 Email настройки

### Gmail SMTP
```bash
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
```

### Другие провайдеры
- **SendGrid**: `smtp.sendgrid.net:587`
- **Mailgun**: `smtp.mailgun.org:587`
- **Amazon SES**: `email-smtp.region.amazonaws.com:587`

## 🔄 Обновление

```bash
# Остановка сервисов
./deploy_fixed.sh stop

# Обновление кода
git pull origin main

# Пересборка и запуск
./deploy_fixed.sh deploy
```

## 🐛 Решение проблем

### Контейнеры не запускаются
```bash
# Проверка логов
docker-compose -f docker-compose.prod.yaml logs [service_name]

# Проверка портов
sudo netstat -tlnp | grep :80
```

### Проблемы с базой данных
```bash
# Проверка подключения
docker-compose -f docker-compose.prod.yaml exec postgres pg_isready -U misago

# Сброс базы данных (ВНИМАНИЕ: удалит данные!)
docker-compose -f docker-compose.prod.yaml down -v
docker volume rm misago_postgres_data
```

### Проблемы с разрешениями
```bash
# Установка правильных разрешений
sudo chown -R $USER:$USER /opt/misago
chmod +x *.sh
```

## 📈 Мониторинг

### Ресурсы сервера
```bash
# Системные ресурсы
htop
iotop
df -h
free -h
```

### Docker ресурсы
```bash
# Статистика контейнеров
docker stats

# Использование диска
docker system df
```

### Мониторинг приложения
```bash
# Nginx метрики (порт 9113)
curl http://localhost:9113/metrics

# Health check
curl http://localhost/health/
```

## 🔧 Конфигурация

### Переменные окружения (.env)
```bash
# Основные настройки
DJANGO_SECRET_KEY=your-secret-key
DJANGO_DEBUG=False
ALLOWED_HOSTS=benj.run.place,84.21.189.163

# База данных
POSTGRES_DB=misago
POSTGRES_USER=misago
POSTGRES_PASSWORD=secure-password

# Redis
REDIS_PASSWORD=secure-redis-password

# Email
EMAIL_HOST=smtp.gmail.com
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
```

## 🎯 Финальная проверка

После развертывания убедитесь что:

1. ✅ Все контейнеры запущены
2. ✅ HTTP endpoints отвечают
3. ✅ Админ панель доступна
4. ✅ Email настройки работают
5. ✅ SSL сертификат установлен (для продакшена)

## 📞 Поддержка

При возникновении проблем:

1. **Проверьте логи**: `docker-compose -f docker-compose.prod.yaml logs`
2. **Проверьте системные ресурсы**: `htop`, `df -h`
3. **Проверьте сетевые подключения**: `netstat -tlnp`
4. **Обратитесь к документации**: https://misago.readthedocs.io

## 📝 Изменения в версии 1.1

### ✅ Исправления
- [x] Опечатка в nginx домене: `run_place` → `run.place`
- [x] Переменная в docker-compose: `POSTGROS_PASSWORD` → `POSTGRES_PASSWORD`
- [x] Создана структура nginx/sites-enabled
- [x] Исправлены права доступа к скриптам

### 🆕 Новые функции
- [x] `deploy_fixed.sh` - улучшенный скрипт деплоя
- [x] `quick_deploy_fixed.sh` - автоматическое развертывание
- [x] `DEPLOYMENT_GUIDE_RU.md` - подробная инструкция
- [x] Расширенная проверка ошибок
- [x] Автоматическое разрешение конфликтов портов
- [x] Улучшенная диагностика проблем

### 🔧 Улучшения
- [x] Лучшая обработка ошибок
- [x] Подробное логирование
- [x] Автоматические проверки системы
- [x] Настройка безопасности по умолчанию
- [x] Документация на русском языке

---

**Разработано**: MiniMax Agent  
**Проект**: Misago Forum Software  
**Дата релиза**: 28 ноября 2025  
**Статус**: Production Ready ✅